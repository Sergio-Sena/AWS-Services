apiVersion: v1
kind: Service
metadata:
  name: backend-node
  namespace: aws-services
  labels:
    app: backend-node
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: backend-node
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
